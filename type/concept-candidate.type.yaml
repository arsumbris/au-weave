#: A mention a source treats as a concept, a defined term, method, or definition. Resolves to a `concept` node.
extends: candidate
fields:
  #: The concept node matched to this mention. Check write diagnostics for kind mismatches.
  #: Filled by weave, or by cut for an enrich candidate's fold target.
  resolves_to?: concept*
